# rough-svgs

Generates rough crayon-style SVG rectangles using Rough.js. Used as navbar button backgrounds on my website.

## Usage

```bash
npm run generate-icons
```

Outputs four 80x30px SVGs. Fill uses `currentColor` and background is transparent.
